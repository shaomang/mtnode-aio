# Key overlay PNGs (BongoCat-compatible)

Put images named after keys, e.g. `A.png`, `Shift.png`, `Space.png`.

- `left-keys/` — left-hand keys
- `right-keys/` — right-hand keys

Custom skins may also include these under `resources/`.
