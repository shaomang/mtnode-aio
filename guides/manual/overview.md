# 从这里开始

> 一句话目标：几分钟内把 MTNode 跑起来——配好服务商、连一条最短的管道、看见第一个结果。

MTNode 把 AI 工作收束到**一张可视化画布**上：**节点**是一个步骤，**连线**是数据流向。输入（文本 / 图像 / 音频 / 视频）、大模型处理、图像生成、音乐与音视频生成、批量生产、任务编排、智能体办事，全部落在这张图上。永久免费开源（MIT 许可），工作流可导出为 `.mtnodes` 包分享。

## 目标

读完本页，你能在十分钟内找到界面上要用的入口、把一个带 API Key 的文本服务商配好、并亲手跑出第一条真实的模型输出。

## 前置条件

- 已安装并启动 MTNode（**不需要先配 Key** 就能打开界面、加节点、连线）。
- 想真正 ▶ 运行，需要一个 OpenAI 兼容服务商的 API Key；图像 / 识图另需对应服务商。

## 步骤

1. **认界面**：先看 [界面在哪儿](#ui-tour)，认全顶栏、画布、左侧 ☰、右侧 ✦、左下角五块。
2. **配服务商**：按 [配置服务商与 API](#providers) 填好 Key，到节点头部 **⚙ 设置** 里确认能选到模型。
3. **连管道**：照 [第一条工作流](#first-run) 加两个节点、连一条线、写一个 `@` 引用，点 ▶。
4. **卡住时**：按现象查 [排错与诊断](#troubleshoot)，不要凭感觉改配置。

## 最常用的五个入口

| 我想… | 去哪一页 | 大约耗时 |
| --- | --- | --- |
| 先在界面上找到按钮 | [界面在哪儿](#ui-tour) | 2 分钟 |
| 让模型真的能跑起来 | [配置服务商与 API](#providers) | 5 分钟 |
| 跑出第一条结果 | [第一条工作流](#first-run) | 3 分钟 |
| 把两个节点连起来 | [加节点、连线与 @ 引用](#nodes-wires) | 3 分钟 |
| 卡住了、报错了 | [排错与诊断](#troubleshoot) | 按现象查表 |

## 其余内容怎么进入

本手册左侧是**章节树**，顶部有**筛选框**；筛选框只按页面标题与章节名过滤左树，**不搜索正文**（正文问答请用页面右上角的「答疑」，它只依据这本手册回答，且不会改画布）。页面之间用 `[标题](#页面id)` 互相跳转，点一下即换页。

- 想知道**某个节点有哪些端口、按钮、会怎么失败**：画布上右键那个节点 → **节点指南**；本手册讲整个应用怎么用。索引见 [节点指南索引](#node-guide)。
- 想知道**某个词什么意思**：查 [名词表](#glossary)。
- 想确认**这本手册本身怎么维护、怎么重新生成**：见 [这本手册怎么维护](#manual-notes)。

**本手册每一页都按同一套结构写**：目标 → 前置条件 → 步骤 → 结果 → 常见错误 → 下一步。赶时间时可以直接跳到「步骤」照着点，遇到问题再看「常见错误」。

> 💡 技巧：第一次使用只读「界面在哪儿」和「配置服务商与 API」两页就够；其余页面等真正用到时再回来查，不必通读。

> ⚠️ 危险：手册里出现的路径一律以本机数据目录为准（Windows 多为 `%APPDATA%\pipeline-console\`）。**应用文件夹（安装目录 / exe 同目录）只读不改**，任何用户数据都不会写在里面。

## 结果

屏幕上出现第一条真实的模型输出；左侧章节树里你能按章节找到上面五张卡之外的任何一页。

## 常见错误

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 不知道从哪页读起 | 手册有 39 页 | 只用上面五张卡；其余等用到再查 |
| 筛选框搜不到正文里的词 | 它只过滤页面标题 | 用 `Ctrl+F` 在画布上搜节点，或用右上角「答疑」问手册 |
| 点链接提示「找不到该文档」 | 链接写成了标题而不是页面 id | 用章节树进入；链接应为 `[标题](#页面id)` |
| 英文界面下部分页面是中文 | 该页英文版缺失时回落中文 | 正常现象，不影响阅读 |

## 下一步

- [界面在哪儿](#ui-tour)
- [配置服务商与 API](#providers)
- [第一条工作流](#first-run)
