# 长周期任务（状态机 · 人在环）

有些活不是一条流水线一次跑完的：中间要你审批、要你补素材、要分头并行、被驳回了还得退回上一步重做。
**长周期任务**就是为这种活准备的——它把整件事画成一张**状态机图**，由应用替你按图推进，
该停的地方停下等你，你处理完它接着跑。

它和画布上的普通流水线**并存**：流水线还照旧一键重跑；长周期任务是另一套东西，
住在你要展开的那根细线里。

## 一、那条细线是什么

菜单条与画布之间有一根 6px 的细线（发丝是中间那条 2px），上下只留一点点空隙、不占地方。
鼠标放上去会亮，**往下拖**就把它展开：画布和页签整体被推到下方，上方这块空间就是长周期任务的
**状态机编辑器 + 实时视图**。细线挂在长任务区的**下沿**，就是它与画布页签之间的那条分界线：
往下拖，线本身跟着鼠标走，页签与画布一路下沉，可以一直拖到**页签贴住窗口下沿**（画布被压成一条）为止——
上限就是此刻窗口真实放得下的高度，不会把页签 / 画布 / 底部状态栏顶到窗外；**拖到下方线也不换颜色**。
往上推回去、或双击细线，就收起。细线的高度与开合**按画布各记各的**：在这张画布拖到哪儿，
切走再切回来还在那儿（别的画布不受影响，另有自己的一份）。

细线还是这条任务的**状态灯**：收起时看不见面板，光看这根线就知道跑得怎么样——
**运行中橙、等你处理（审批 / 交付）黄、卡住或失败红**，这三种走呼吸灯；
**跑完绿**，常亮不闪。这张画布还没有长任务时，它就是一根还没挂任务的普通细线（一旦新建，见下段）。

**新建即成图**：长周期任务**一经创建就显示在这根线里**——不用先在条带上点「▶ 启用并绑定」才看得见。
创建那一刻，条带会**自动展开**并摊出这张图（展开态按画布记着：切走再切回来还是展开的），
同时在主画布上把这条任务的落点一次摆好（父壳 + 各环节子壳 + 生成工作流 + 交付节点，见五之二）；
若这条任务是在**别的画布**上建出来的（会话 / 助手可能这么干），不会把你正看着的这张画布硬掀开，
只落盘并提示一句，切到它所属的那张画布就能看见。
**创建只建不跑**：任务建成时仍是「未启用」，生成一个都不会自动运行——开跑只由你点 ▶ 启用并绑定。

条带右端是运行控制（**▶ 启用并绑定 / ▶ 继续 / ■ 停止 / 重新启用 / 停用解绑**）与「记忆」「⚙」；
**任务切换、＋ 新建、历史 run、交付目录体检**都在 ⚙ 里。
不过**换任务不必翻 ⚙**：状态机图画布**左上角面包屑的第一格写的就是你正看着的这张长任务名**
（不是写死的「主图」），**点它**就摊出一份任务清单 —— 换一张旧长任务、或就地新建一张；
当前这张带 ✓ 且点不动，任务名旁边标着节点数 / 图版本 / 是否已绑定，鼠标停一下就能看见。
**这张画布还没有长任务时，展开的那块空间就是新建入口。**

**新建长任务**开的是一只近全屏窗（可最小化到状态栏；右下角可拖调大小，最小宽 = 视口一半）。
两条路都在窗里：左边给 Agent 写一两句目标，让它先「拷问」你把需求问清楚、再把图落库；
右上角 **＋ 手动新建** 则直接从一份空白模板（起点 → Agent 任务 → 完成）起步。
窗顶的 **Agent 选型**（服务商 / 模型 / 预设 / 思考强度）决定这次创建给**整张图的 Agent 环节**继承什么：
全部留空 = 跟随默认，建成后仍可在节点检查器里逐个节点改。
右上角 **取消**（或 Esc）只关窗：**你写的正文与这套选型都会留着**，下次打开本窗原样回来。

## 二、图里都有哪几类环节

| 环节 | 做什么 |
| --- | --- |
| 起点 / 成功终点 / 失败终点 | 一条路走到底就收工；到失败终点这一路算失败 |
| **Agent 任务** | 交给智能体做：填目标、吃哪些状态键、产出哪些状态键，可指定模型与预设 |
| **人工任务** | 停下来等人：**审批**（通过 / 驳回）或 **内容交付**（按清单交东西） |
| 汇聚 | 并行分支等齐再走：全部到齐（AND）或任一到达（OR） |
| 选路 | 按条件走其中一条边（也可以几条同时走 = 并行） |
| 逐项并行 | 把一个数组摊开，每一项跑一遍内部子图，各项互相看不见 |
| 子图 | 把一段流程装进一个容器，双击下钻进去编辑，可以一层层套下去（面包屑上的「← 返回外层」逐层退回，退到最外层图它自己收起） |
| 写文件 | 把某个状态键落成一份文件 |

连线可以写**条件**（一小段受限 JS，读得到整份共享状态：写 `input.state.<键>`，也可以直接写
`state.<键>`；另有 `input.node`（本边起点这一环）与 `input.runId`。写法随意：`return 真值` 或
直接写一行表达式（如 `state.ok === false`）都认，返回真值才放行；写错或超时会转成
「需人工」而不是把任务崩掉）。

### 状态键是**一份共享状态**：写文件环节会自动兜底

各环节产出的状态键都在同一份任务状态里，**平级环节之间也读得到**（只有「逐项并行」的各个实例
互相看不见 —— 那是刻意的隔离）。所以「写文件」环节只要在下拉里挑中某个键，不管它是上游写的
还是平级环节写回来的，都取得到；Agent 环节的提示词里也能看到这些键。

万一真的取不到，写文件环节先**自动兜底**，实在兜不住才判失败：

- **键写在别的环节那一层**：引擎按整份共享状态再找一遍，取自别的环节时在运行日志里点名
  「已自动修复：状态键「x」取自 〈环节〉」—— 绝不悄悄换料。
- **没填落盘路径**：自动落到 `<画布工作目录>/<本环节标题>.md`，日志里写明落在哪。
- 两样都取不到才判失败，失败说明里会列出**现在可见的状态键**：按它把「取哪个状态键 /
  写到哪个文件」填对，再点「重跑这一环」即可。
- **已经失败过的落盘环节**：点「▶ 继续」时，只要它的状态键现在取得到，引擎会把它**自动重新排队**
  （只写一个文件、不花 Token；你手动放行过的环节不动 —— 你的判断不推翻）。
- 图里没填键 / 路径的写文件环节，启用前的校验会先给一条提醒（提醒不拦启用）。

### 某一环报错了？可以直接无视它往下走

任何一环**报错**（Agent 重试用尽、条件谓词写错 / 超时、子图里跑坏、写文件失败），右栏「**卡住的环节**」
里就会出现它的一张卡。除了「重跑这一环」，卡上还有一条**出路**（选中那一环时，检查器顶上也是同一只控件）：

- **无视这次报错并继续**：把这一环记成「已跳过」，**当场点火它的下游** —— 任务接着往下跑，
  不必先修好它。它已经留下的东西（产出节点里的正文、落盘的文件）下游照旧读得到；
  真缺的东西由下游那一环自己说（会标「需人工」并写清缺什么）。
- **跳到哪一环**（可选）：填了就**只跳去那一环**（下拉里列着同一张图里的其它环节），
  不填就走它自己的下游。
- **判失败也继续**：第二档出路 —— 这一环照旧记「失败」（不假装成功），但同样点火下游，
  只是运行档案里留着这次失败。适合「这一步没做出来，但我不想卡在这儿」的判断。
- **以后这一环报错都照此放行**（勾上）：写回**图定义**（`onError` / `onErrorNext`），
  以后它的报错就自动按上面的口径放行，不再停下来等你。

放行过的环节在右栏仍列着（灰琥珀色，写着「已放行」与原因），你随时能看出哪一环没按预期跑完；
想重做它，点那张卡上的「重跑这一环」。**跳过 ≠ 成功**：状态机照图继续收敛，最终成败仍按图算
（走到成功终点才算成功）。

### 整条任务推不动了？点「⏭ 强行进入下一状态」

上面那条出路管的是**某一环**。任务整条推不动时（报错停住、你按过「■ 停止」、状态收成
「卡住」），条带右端还有一颗 run 级的出路：**⏭ 强行进入下一状态**（跑着 / 跑完时不出现）。

点它先弹一次确认，说清会发生什么：

- **报错停住的环节**：按上面的「放行」同一套口径处理 —— 记为**已跳过**、当场点火下游，
  不假装它做成了；它自己填了「跳到哪一环」的，就按那个跳。
- **被「■ 停止」按下来的环节**：**排回队列接着跑**（停止 ≠ 报错：它不是坏掉，是被你叫停的，
  所以恢复的是「重跑」而不是「跳过」）。
- **正在等你确认的环节不动**：审批 / 交付要你自己判断，这条出路**不代你点确认** ——
  那两件事仍在右栏的卡片上处理。

顺手说一句：**「缺起点 / 缺终点」这类图问题不会再拦住你**。点「▶ 启用并绑定」时，图里缺的那一头
（没有 `start`、没有 `end_ok`）会被**就地补出来**（补出来的是普通环节，你在图上照样能改），
补不动（一个节点都没有）才会明确告诉你原因。所以「图校验不过 → 根本开不了跑」这件事不再发生。

**加环节**：在状态机画布上**右键**即可（空白处、节点上、连线上都行），菜单里就是上面这十类环节；
**删环节 / 删连线**：先点选它（右键点它也算选中），再**直接按 Delete 键**（或从右键菜单里点「删除选中」）。
以前这两件事是画布上方一排按钮，现在整体收进了右键菜单 —— 条带不再被按钮挤满。

### 每个环节在画布上列出自己的子任务

不用点开检查器，看卡片就行——每张卡片在环节名与摘要下面逐条列出这一环的**子任务**：

- **🤖 = AI 自动做**：Agent 任务的目标 / 步骤（`goal` 里换行或写成「1. 2.」的列项会拆成多条）。
- **✋ = 需要人工干涉**：审批环节、以及交付清单里还没交的条目，都用琥珀色标出来，一眼看出卡在谁身上。
- **☐ / ☑**：交付清单逐条列 （未完成 / 已交付）。
- **⤵**：子图 / 逐项并行的内部成员（成员需要人工的同样标琥珀色）。
- **⚠ / ▸**：这一环的报错与正在跑的实时输出。

卡片高度**跟着子任务条数长**，连线端点也一起跟着走，所以列再多也不会把线连到卡片里。

每张卡片的文字按**卡片宽度截断**，不会再探出卡片外面；想看更多说明就拖这张卡片**右下角**的小手柄
把它拉大——拉宽后文字按新的宽度重新排，能多显示几行，卡片大小随画布一起存盘。

### 缩放

在状态机画布上**滚轮就能缩放**（以光标底下那一点为锚，放大时不会跑偏；25%–400%）。
画布**左上角那一行**（面包屑「当前长任务名 ▾ ▸ …」那一行的右端）那枚百分数既是回显、也是**复位键**：点一下回到 100%。
缩放**按画布各记各的**，切走再切回来还是你上次的视野。

## 三、启用与绑定

落到这张画布上的长任务，**一建好就在条带上显示**（图已摊开、画布上的壳 / 生成工作流 / 交付节点也已摆好），
**启用只决定开跑**：创建不代跑，点条带上的 **▶ 启用并绑定**，才从这一刻开始跑。启用后：

- 这个长任务**绑定当前画布**；每个 Agent 环节上勾了「允许读取画布」才拿得到画布内容（`mtnode_canvas_get`），
  而且是**只读**：这一轮改图与应用类画布工具根本不注册，调用即失败（要改画布你自己在画布上改）。
- 启用那一刻拍一张**图的快照**当作本次运行（run）的骨架。你之后改图，**不影响正在跑的这一次**；
  想让改动生效，就点「重新启用（新 run）」。
- 应用重启后自动恢复到中断处的**现场**，但**不会自动重跑**——烧 token 的事留给你点「继续」。
- 一张画布同一时刻只有一个启用中的长任务，可以在 **⚙** 里切换。

## 四、人工任务怎么处理

条带右栏会列出「等你处理」的卡片：

- **审批**：填意见 → ✓ 通过 继续往下；✗ 驳回 会退回你指定的环节重做。驳回**必须写理由**，
  理由会写进共享状态，上游那个 Agent 下一轮读得到。理由框里**写到一半的内容会一直留着**——
  任务在跑的时候条带会随进度重绘，重绘前后你写下的字原样还在（点别处失焦也不会被清空），
  右栏里**正在打字、正在框选、鼠标正按着**的那一段都原地不动（选中的文字不会被重绘冲掉、
  框选到一半也不会被打断）；只有点了 ✓ 通过 / ✗ 驳回 才算提交、才清空，换画布也会清掉。
  回跳**默认不限制次数**（可以一直退回重做）；
  若在环节检查器或 ⚙ 里设了上限，超出上限时这一环会转「失败」，绝不无限循环。
  驳回后旧版曾出现「整张图停成卡住」的情况（回跳把入口点火也一并撤了，目标环节再也起不来）：
  现已修好——新跑不会再有；**已经卡住的旧现场**点一次「▶ 继续」就会把那些被跳过的环节重新排队接着跑。
- **内容交付**：卡片上是一条条清单（文件 / 文本 / 选项确认 / 媒体，可分必填选填）。
  文件可以「上传」，也可以在画布上把上游节点连到交付节点 ——
  **连进来的文件只要真拿得到（上游已产出、文件在磁盘上）就自动收下**：端子消失、
  文件复制进交付目录、清单记「已交付（连线）」，不必再手动点「从画布连线取」。
  收线在接线那一刻、任务每跑完一步、打开 / 切回画布与条带重绘时都会发生；
  上游还没产出 / 拿不到文件的那几项照旧待交（系统绝不凭空算已交）。
  Agent 干活时若觉得你还得补东西，会**追加**候选条目——那种条目带橙色标边，要你逐条
  **接受**或**驳回**，不接受就不算要求。**放行只有一条路：你在卡片上点「确认交付完成」**——
  连线交齐的件只是自动记为已交付（省掉再去收一次线这一步），**收齐不等于放行**；
  **缺件时点它**会先弹确认窗把没交的条目列出来、要你写清原因才放行。
  交付以文件为单位：**文件项要有一个带后缀的文件名**（例 `分镜表.md`），没写文件名、或只拿一句
  描述充数，清单里就显式标**「文件名待补」**（描述不会被当成文件名收下）。
- **没交齐也能继续，但要说清为什么**（本轮口径）：必填项没交齐、或必填文件还没定下文件名时，
  点「确认交付完成」**不再被拦住**——先弹一只确认窗，把**没交的条目逐条列出来**
  （「没交」/「没定下文件名（含后缀）」），下面给你一格**交付说明**：为什么有些没交付、
  哪些需要额外交付（一件都没交也能继续）。窗里两个出口：
  **「继续任务」**＝照这个说明放行、任务往下跑；**「返回补齐」**＝什么都不提交，说明留在草稿里
  （再点开还在），你回去继续交。说明**可以留空**，但空说明在交付目录里只会记成「（未填说明）」。
  放行过的条目在清单里标**「未交付·已放行」**，卡片上也会写明「已在未交齐的情况下放行 N 轮」。
  每一轮的放行都**逐轮留痕**：写进共享状态（`deliver_note` / `deliver_missing`）、写进 run 的
  逐轮记录、也写进交付目录 —— `manifest.json` 的 `releases` 与《交付清单.md》的
  **「未交付说明」**段（应用外也看得见，含轮次 / 时间 / 未交清单 / 你的说明）。
  交付环节**之后**的 Agent 环节会在提示词里读到这段说明，并被要求：缺件会影响这一环的产出就把它
  标成**「需人工」**并说清缺什么、要谁补，别硬跑。

交付节点（主画布上那个）就是这条人工任务的**落点**，它本身也能改清单与交文件（见下一段）：
**任务创建时它就已经摆在画布上了**（交付目录与清单同时落到该画布工作目录），不必等跑到那一步才看见；
节点上改过之后**以画布上的为准**，条带卡片与它读的是同一份清单，不会两处各写一半。
画布侧（节点上、条带卡片上）的增删改还会**回写任务图的清单定义**，所以**下次启用 / 重跑不再读
旧清单**（切画布、重开画布恢复现场时也会补齐）；正在跑的这一轮仍用启用那刻的快照。

**一个端子严格对应一个文件**：清单里每个还没交的文件就是节点上的一个**输入端子**，端子标签
就是文件名；节点板身顶部那张 Markdown 表逐行写明每个端子对应的文件**要写什么**（文件名 ·
内容说明 · 必填/选填 · 格式或大小要求 · 交付状态）。**文件交掉后该端子就消失**，其余端子按序
补上，已连的线跟着自己那个文件走。每个端子后面有「⬆ 上传」（选的文件名要跟端子对得上；
对不上会红字提醒但照样收下）与「删除」；下面的**已交区**能「撤回」——只把端子放回来，
交付目录里的文件一律不删。格式 / 大小要求也**只提示不拦**。

清单是**双向**的：应用写 `manifest.json`，也读得回来 —— 你在应用外（资源管理器 / 编辑器）手改过的
`done`、填过的值、丢进交付目录的附件，在**重新进入这一环**或点「继续」时会被认回来，不会当成没交。
不过**重跑同一环节算新一轮**：清单重施注入，所有端子都回来，需要重新交一遍（上一轮的文件仍在交付
目录里，可以随时回看）。

## 五、产出节点：执行中生成的内容都写进画布

长任务跑到一半，**每个环节生成的信息与文件内容都会同步写进画布上的「产出节点」**
（标题形如「产出 · 起草」）——它落在**该环节的超级节点子壳里**（见五之二；任务创建时壳已先摆好），
不用等整件事跑完，你随时能在画布上查阅，也能直接改。

- **正文可直接编辑**：在节点板身上改，或点节点头部的 **✎** 用内置 Markdown 编辑器改
  （保存即写回节点）；节点下方按只读列出这一环落盘的文件（文件名 + 大小，
  鼠标悬停看全路径）。
- **你改的内容算数**：后续环节接着跑之前会核对产出有没有被你改过——
  - 正文与登记时不一样、或文件被改动（时间 / 大小对不上）→ 后续环节**以你改后的现内容为准**
    （你改的内容算数），不拿旧版覆盖你；
  - 一切都对得上 → 照常往下跑；
  - **核对不上**（产出节点被删了、文件读不到、这一环还没登记过产出）→ 系统不猜：条带上出现一张
    「产出判不准，请确认」的人工卡，你答完并点确认它才接着跑。
- **一个环节只有一颗产出节点**：重跑 / 回跳都复用它，你删了下次跑到会按同一身份补回来；
  它由系统创建，**不能手动新建、不能复制**，建它也不进撤销栈（按 Ctrl+Z 不会把它撤掉）。
- 产出节点与画布一起存盘，关掉应用再打开还在。

### 末尾再逐件摆出「产物节点」

产出节点一根记的是一环的正文 + 文件清单，清单长了还得挨行看。所以**每个环节跑完时，
系统会把这一环这次真正写出来的产物再清点一遍、**只挑你该读 / 该收的关键文件**，
一件一件摆成主画布上的「产物节点」**（标题形如 `产物 · 文本 · pilot.md`），就摆在同环节
产出节点的右侧，按空位网格排开、不压住你原有的节点：

- **看得到**：图片给缩略图（点开看大图 / 另存为），文本 / Markdown / 表格给一段摘要，
  其它类型给路径。节点头部 **⇢** 用系统默认程序打开，底部一行是完整路径。
- **直接改**：文本类产物的头部有 **✎ 编辑保存** —— 点开应用内可编辑阅读器改几笔，
  **保存即写回这件产物文件**，节点上的体积与摘要随即刷新；想看全文也在这一枚里
  （阅读器自带 Markdown 渲染），读完顺手就能改。
- **只摆本环节自己产出的**：上游环节交下来的输入文件不会在下游再摆一遍，免得每个环节都把
  上游的图重复铺一屏。单次最多摆 32 件。
- **只摆关键文件**（画布留给结果，不留给过程）：报告与文档（`.md` / `.pdf` / `.txt` / `.html` /
  `.docx` / `.pptx` / 字幕…）、数据表（`.csv` / `.xlsx`…）、成品图（`.png` / `.jpg`…）会摆；
  视频与音频、日志与配置、中间件（分镜 JSON、抽帧图…）、依赖锁、源码 / 脚本 / 归档一律不占画布。
  没摆的那些**没有丢**：仍在工作目录里，产出节点的文件引用清单也一件不少；交付环节你亲手确认过的
  交付件照旧全摆。
- **怎么算「这一环写出来的」**：Agent 用写文件工具（write / edit 这类）落盘的每个路径都会
  被记下来 —— 它给的是**工作区相对路径**（`assets/H3提示词/shot-01.md`）也算，按本画布的
  工作目录解析；写回状态里的路径（整条或夹在一段说明文字里的）同样会被抽出来、逐条核对
  文件真的在。万一产物没走工具入参（外部程序直接把文件写进工作目录），还会退回按**本环节
  的时间窗**扫一遍工作目录兜底。读不到的文件一律不摆，免得画布上留一张死卡。
- **一个产物一颗、认人复用**：按「任务 + 环节 + 文件路径」认人，重跑 / 回跳不会堆新的；
  可以随便拖动、改名、删除，删了下次跑到那一环会补回来。
- **旧任务会补摆**：升级前跑过的环节没留下产物清单，打开这张画布（或点「继续」）时系统会按
  该环节自己的时间窗把这一轮写出来的文件补摆一次，不用为了看见产物再空跑一轮。
- 与产出节点、交付节点同源：**系统建，不能手动新建、不能复制**，建它也不进撤销栈。

## 五之二、产出都收在「超级节点壳」里

每个环节的产出不该散在画布上，所以长任务给**每条任务在主画布上建一颗超级节点**（壳名形如
「长任务 · <任务名>」，头部一枚粉紫色的「长任务」徽标，**创建任务时就建好**），**每个环节在它内部再占一颗子壳**
（子壳名 = 环节标题，同样是创建时先把壳摆好）。产出节点与产物节点都落在**对应环节的子壳里**——
点开壳就看见这一环这次跑出了什么，不占用主画布。

- **创建即预建、只建不跑**：任务一建好，父壳 + 各环节子壳（+ 该环节的生成工作流）就摆在画布上，
  不必等它先跑到哪一环；预建**只建节点与连线，不跑任何东西**。
- **按身份认人**：壳上记着「属于哪条长任务、哪个环节」，重跑 / 回跳 / 重启都按任务身份（任务 uid）
  与环节路径复用同一颗，不会每跑一轮堆一颗；你后来在图里**新增的环节**，它的壳（与生成工作流）
  会在那一环第一次真的产出东西时才建，同样只建不跑。
- **与任务绑定**：不需要你手工绑定；条带任务卡上也没有
  多余入口 —— 直接用画布上的壳就行。删掉长任务时**壳与壳里的内容都保留**（只清掉壳上的任务
  绑定字段），所以壳里你改过的东西不会跟着任务一起消失。
- **删了不会偷偷重长**：你手动把壳（或某颗环节子壳）删掉后，引擎不会静默重建 —— 任务日志记一条、
  界面上提示一次，那一轮的产出先回落主画布层；想要壳回来，在条带上点「重建壳」。
- **壳内可自由编辑**：与普通超级节点一样，可以往里加自己的节点、挪位置、改标题（双击卡片进入子画布）。
- **旧画布的产出**：升级前已摊在主画布上的产出节点，在打开这张画布或点「继续」时会被收进对应
  环节子壳（产物节点不动 —— 你可能已经改过它们）。

### 涉及内容生成时：工作流给你建好，但**生成由你点**

图像 / 视频 / 音乐 / 配音这类生成是要花钱、花额度、吃显存的，所以系统的口径是**只建图、不代跑**：

- 创建任务时，环节在条带检查器里勾着「需要生成内容」（新任务默认勾上）时，它就在**该环节的子壳里
  摆好生成工作流**：按「生成类型」多选（图像 / 视频 / 音乐 / 语音，默认只勾图像）
  各摆一个生成节点，**平行摆开**（不是串成一条链），提示词按环节标题 + 环节目标自动写好、你可以直接改，
  控制节点摆在靠上（**▶ 由我执行生成**）、生成节点靠下。
  （你后来在图里新增的环节，见五之二：它跑到、产出真的落进子壳那一刻再补建；已建过的同类型节点直接复用。）
- **一律不运行**：预置只建节点与连线，一个生成都不会自动跑。你进壳里看清提示词与参数、确认要花这笔钱，
  再点生成节点的 **▶** 开始生成。产出节点与交付节点同一口径：创建时就把落点摆好，但都只建不跑。
- 本地后端没装也照建（未就绪的节点自己红字提示、点 ▶ 会硬停），不会替你「先偷偷试一下」。
- 生成完了不用手动归档：产物清点会按路径把结果摆进这一环的子壳，与其它产物同一套。
- 生成**不阻塞任务**：你暂时不想生成，任务照常往下跑；生成是补充素材，不是必须交的件。

## 六、长期记忆

条带的「记忆」里是这套系统的长期记忆库（SQLite + 全文检索），分**全局 / 项目 / 当前画布**三层，
每个环节注入时按 当前画布 → 项目 → 全局 逐层补足（默认 TopK 8 条），带回出处。

- 环节跑完会**自动抽取**候选事实 → 条带上出现「待确认记忆 N」→ 你逐条接受才入库（可以全收 / 全驳）。
- 也可以手动记一条，或让 Agent 用 `lt_memory` 主动写（它被要求：自己推出来的一律走"提议"）。
- 检索：英文 / 词串走全文索引；**含汉字的关键词按子串匹配**（默认分词器会把一整串连续汉字当成一个词，
  「验收」那样查是查不到「长任务验收」的），所以中文检索是「包含就算命中」。
- **与专家团的事实库双向同步**（默认口径就是**本画布那一份事实库**，不是随便挑个目录）：
  - **导入**「从专家团事实库导入」：读本画布库里的每篇文档，按 1~3 级标题切成可检索条目入记忆库。
    条目 id 由「画布 + 文档名 + 标题」定死，所以**反复导入是刷新，不是越导越多**。
  - **导出**「导出到专家团事实库」：写进库里固定的一篇《长任务记忆沉淀》（同一标题只留最先写进去的那份，
    不会覆盖专家的原文）；写盘即通知左栏刷新。
  - 库还没建、或要跟外部（第三方）Markdown 库对接时，用旁边的「从外部目录导入 / 导出到外部目录」
    作为第二入口；外部目录导出按天命名，且**先读回再追加**，同一天导两次不会把前一次覆盖掉。
  事实库仍是专家团那边的真源，这里是给长任务用的检索面。

记忆只是提示，不是事实：当它与你的文件或事实库文档冲突时，系统会照实说，不会偷偷覆盖你写的东西。

## 七、并行、重试与阈值

条带右端 **⚙** 里放着两样：**这张画布的长任务**（切换 / ＋ 新建 / 历史 run / 交付目录体检）
与跨画布统一的全局阈值：图级并行度（默认 4）、Agent 失败重试（默认 2）、
驳回回跳上限（默认 0 = 不限）、记忆注入 TopK（默认 8）。单个环节上还能各自覆盖前几项。

并行度是**全图 Agent 环节的总闸**：逐项并行（map）摊开的每个实例也共用这一个额度 ——
摊 16 项、并行度 4，同时最多仍是 4 个 Agent 在跑，其余排队（排队中的环节在图上显示为「在跑」）。
容器环节（子图 / 逐项并行 / 汇聚）本身不占额度，所以不会出现「父环节占着额度等子环节」的卡死。

Agent 环节跑完了、却**没把它声明的输出键写回状态**时不会立刻判失败：先在同一条会话里补问一句
「只补写回、不要重做任务」，最多 2 轮（补问时会把本轮已落盘的产物文件摆给模型，路径类键从那里取）；
补不齐才判失败。补问过程会写进条带日志与运行轨迹，看得见发生了什么，不会悄悄多烧几轮。

## 八、历史运行与体检

- **图定义不走 JSON 文件**：导入 / 导出入口已经移除，图就在条带里编辑、随画布一起存盘。
- 「⚙ → 历史 run」列出这台机器上这张画布跑过的每一次运行，可切回某个 checkpoint 继续。
- 「⚙ → 交付目录体检」只**报告**已经没有对应任务的交付目录，绝不自动删你的文件。

## 八之二、任务链修改（用一句话让 Agent 改图）

条带头部那颗 **✎ 修改任务链** 开的是一只大窗：顶部写一句你要改什么，点「发送」，
Agent 会先读**当前这张任务图 + 运行态**（跑到哪、哪个环节卡住、正在等谁），再按你的要求
**原地**改这张图（增删环节、改目标 / 输出键、加条件分支、拆解子图、补兜底路径…），改完写回
并给一段「改了什么」的摘要。

> 每次「发送」都**重启**：另起一条全新会话，不继承前面的上下文（避免旧上下文干扰新要求）。
> 上一次那条留在左侧栏只作历史，不会被本窗接回。

- 走的是**同一张任务**：uid / 任务名 / 启用状态 / 历史 run 都保留，图的版本号 +1（图定义换了）。
- **正在跑的 run 不受影响**：它拿的是启用那刻的快照（与条带里手改图同口径）。要让新图跑起来，
  在条带「⋯ 更多 → 重新启用（新 run）」或「▶ 启用并绑定」。
- **每次「发送」都重启会话**：新要求另起一条全新的会话，不继承前面的上下文（改任务链最怕旧
  上下文干扰新要求）。上一次那条会留在左侧栏，只作历史可回看，不会被本窗接回；没发出去的要求
  仍然原样留着。
- 大改之前建议先「⚙ → 历史 run」看一眼当前版本，改错了还能切回旧 checkpoint。

顺带一提：条带头部那些**几乎用不上的手动按钮**（启用并绑定 / 重新启用 / 停用解绑 / 记忆 /
历史 run / 交付目录体检 / 任务图校验 / 删除任务）现在都收进了 **⋯ 更多** 这一颗下拉里，
常驻只剩真正用得上的几个（继续 / 停止 / ✎ 修改任务链 / ⚙ / ✕）。

## 九、要注意的

- 交付目录在 `<画布工作目录>\mtnode-deliverables\<uid>\`（uid 形如 `ltxxxxxxx-交稿`，短名可含中文，
  目录名一眼能认出是哪个环节）；工作目录不可用时退回应用数据目录并提示一次。
- 一切用户数据都只在工作目录与 `%APPDATA%\pipeline-console\longtask\` 下，**不会写进应用安装目录**。
- 交付节点可以删——删了那条人工任务会被标成需要重建，下次跑到那环时系统会重新建一个（uid 不变）。
  但它不能复制，也不能手动新建。系统建它时**不进撤销栈**：在画布上按 Ctrl+Z 不会把正等你的
  交付节点撤掉（真撤掉了 run 还停在等交付，画布上却没有落点）。
