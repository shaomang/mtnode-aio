# Task graph (moved)

This page was merged into **[Control flow & judges](#control-flow)**.

Task nodes, the judge node, and the whole control-node family (timer, delay, sequencer, gate, splitter, counter, mutex, wait-file, success / fail ends) now live on that one page. Update your bookmarks.
