# Gate, split, mutex (moved)

This page was merged into **[Control flow & judges](#control-flow)**.

Gate (AND), splitter, sequencer, counter, mutex and wait-file are documented there together with the judge node and the task endpoints.
