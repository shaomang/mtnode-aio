# 节点指南

本目录为画布节点右键「节点指南」的内容源。

| 路径 | 说明 |
|------|------|
| `*.md` | 中文指南（默认） |
| `en/*.md` | 英文指南（可选；缺省回退中文） |
| `img/<id>.svg` | 对应图示。可用 PNG 替换，保持同名即可 |

图示可随时覆盖。若只需占位，保留现有 SVG（带 `placeholder` 标注）。

指南 ID 与节点对应见 `index.json`。
