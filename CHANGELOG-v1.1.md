# MTNode AI编排器 · v1.1 更新文档

> 覆盖范围：v1.0.25 → v1.1.28（当前版本）
> 本文档汇总自 v1.0.25 起全部版本更新内容，是 v1.1 大版本（含前置 1.0.x 补丁版本）的完整变更记录。

---

## 爆肝48小时，我将 Deepseek Harness（dsh）植入 AI 编排器后发现了无限可能。

## 一、版本总览

| 版本    | 主题                                                                     |
| ------- | ------------------------------------------------------------------------ |
| v1.0.26 | 引入 DeepSeek Harness（dsh）智能引擎：智能任务节点 / 智能模式 / 插件管理 |
| v1.0.27 | 智能任务节点全面对齐文本处理节点；Node 运行时随包内置，零配置            |
| v1.0.28 | 工作流多标签页；Agent 预设 / 对话发送行为配置；插件启停                  |
| v1.1.0  | 全屏智能会话画布；多供应商路由；技能 / MCP / 权限预设；10 款主题         |
| v1.1.1  | 图像输入 + 视觉模型；AI 助手可直接搭建工作流；在线扩展商店               |
| v1.1.25 | 超级节点收纳 / 展开进入 / 边端子 / 子文件夹路径；边栏超级节点树         |
| v1.1.28 | 开发节点「建议 / 开发 / 细化」；数据库节点 + mtnode_db；执行 / 音乐 / 视频 / 网络 / 全局节点 |

**一句话概括 v1.0.25 → v1.1.25**：从"调用 API 生成文字 / 图像的画布工具"进化为"自带 DeepSeek Harness（dsh）智能引擎、支持 Agent 会话、视觉模型、多供应商、可安装技能 / MCP 插件、可由 AI 直接搭建工作流，并以超级节点收纳复杂子图的完整 Agent 编排器"。

---

## 二、v1.0.26 —— 智能能力（dsh）首次引入

**核心：应用内置 DeepSeek Harness（dsh）Agent 网关，让节点不再只是"调 API"，而是能读文件、联网、执行命令的 Agent。**

### 新增功能

- **「智能任务」节点（`agent_task`）**：新增「智能节点」分类与右键菜单入口；节点内填写任务描述 + 工作目录，运行后由 dsh 引擎执行，可读文件 / 联网 / 执行命令；输出可作文本连线
- **dsh 引擎网关**：主进程接入 `dsh/main-dsh.js` 适配器（懒加载单例），配置项：启用开关 / 默认模型（默认 deepseek-v4-flash）/ 最大 token / 默认工作目录；新增 IPC：`dsh:config` `dsh:status` `dsh:run` `dsh:pluginList` `dsh:pluginAdd` `dsh:pluginRemove`，事件实时推送（reasoning / text / tool / status / title / usage / error / done），运行日志写 dsh.log
- **文本处理节点「智能」按钮**：开启后提示词变成任务，Agent 可读文件 / 联网 / 执行命令（需 DeepSeek 服务商）
- **对话节点「智能助手」开关**：微信风格气泡 + 流式回复，历史消息串行化为上下文（最多 20 条）
- **设置新增「智能能力」区块**：总开关、Node 路径（≥22.19）、默认模型、引擎状态检查、插件安装 / 移除（引擎自动重启）
- **帮助文档新增「智能能力（dsh）」章节**
- **智能模式批量限流**：并发 2 逐条执行，避免同时开过多 Agent 轮次

### 修复与兼容

- 智能运行失败错误落入节点 error 槽并 toast
- 退出时关闭 dsh 引擎，避免遗留孤儿子进程
- 旧工作流迁移：proc_text / chat 补 `agent`、`agentWorkspace`、`messages` 等默认字段

---

## 三、v1.0.27 —— 智能任务整合与零配置化

**核心：智能任务节点并入统一节点体系，接入门槛大幅降低。**

### 新增功能

- **智能任务节点功能对齐文本处理节点**：支持 `@` 引用、多输入连线、批量 / 聚合模式、模型选择面板、API 预览、上游自动执行
- **任务摘要预览**：预览弹窗展示路由 / 模型 / 工作目录 / 输入节点 / 批量模式 / 任务内容，可一键复制
- **文件夹选择器**：工作目录与设置中「默认工作目录」均可点「浏览」选择目录（新增 `file:openDialog` 目录模式）
- **dsh 引擎随应用自动启动**（幂等、失败不阻塞）

### 改进与修复

- 移除「Node 可执行文件路径」手动配置——**Node 运行时随应用自带**，无需单独安装
- 智能任务固定走 DeepSeek 路由，隐藏服务商下拉（避免误配置），仅保留模型选择，且模型选择在智能模式生效
- 修复智能模式误触发视觉校验问题（智能模式不再要求服务商支持 vision）
- 「智能任务不支持中途取消」提示与 UI 先行复位
- 智能任务空描述校验 toast
- 旧存档迁移补 `batchMode` 默认值

---

## 四、v1.0.28 —— 工作流多标签与智能能力配置增强

**核心：Edge 风格多标签页工作流管理 + 智能引擎配置自由化。**

### 新增功能

- **工作流多标签页**：访问过的工作流以 Tab 形式显示（上限 12 个），点击切换、「×」关闭标签（不删工作流）、「＋」新建；原顶栏工作流下拉框移除
- **Agent 预设**：通用助手（默认）/ 精简执行 / 代码专家 / Cordis 插件开发助手，随智能任务传给引擎
- **插件启停**：每个插件可「启用 / 停用」（新 IPC `dsh:pluginSetEnabled`）；插件列表区分用户插件与可折叠的「运行时内置插件」
- **对话节点发送行为设置**：Enter 发送·Shift+Enter 换行，或 Enter 换行·Ctrl+Enter 发送（`chatEnter` 配置）
- 设置页新增「打开存档位置」「查看说明」按钮（顶栏对应按钮移除）

### 改进

- 顶栏精简：立即保存 / 存档位置 / 说明 / 版本号按钮移入设置页，画布操作提示移入 logo 副标题
- 插件列表加载失败自动重试 2 次
- 工作流重命名实时同步标签标题

---

## 五、v1.1.0 —— 智能会话画布（大版本核心）

**核心：从"画布编排工具"升级为完整的 DeepSeek Harness（dsh）客户端。**

### 新增功能

- **全屏「智能会话」画布**：顶栏「编排 / 会话」双视图切换
  - **多会话管理**：会话持久化（上限 60 个，每会话保留最近 100 条消息），按工作目录分组，支持分支 / 归档恢复 / 删除
  - **会话头部配置**：预设、供应商（DeepSeek 官方 / MTNode 服务商 / pi-ai 目录）、模型、思考强度（无 / 标准 / 最强）、工作目录
  - **斜杠命令**：`/new` 新会话、`/compact` 压缩上下文、`/plan` 先规划后执行、`/rename`、`/export` 导出会话、`/permissions`、`/help`
  - **流式运行 UI**：实时思考块、工具调用 chip（参数 / 结果可展开）、运行中可「终止」
  - 旧单会话数据自动迁移
- **智能任务节点 ↔ 智能会话双向同步**：节点「↗」扩展为会话；节点运行后同步到会话，会话改动回写节点（标题 / 参数 / 最新回复）；删除关联会话的节点需确认
- **多供应商路由**：DeepSeek 官方 / MTNode 自配服务商（经网关注入 key）/ pi-ai 目录；新增「服务商目录」搜索卡片（模型 ID / 名称 / 上下文窗口）
- **思考强度（effort）**：文本节点智能模式 无 / 低 / 中 / 高 → 引擎三档；智能任务 / 会话直接 无 / 标准 / 最强
- **工作流级统一工作目录**：画布工具栏设置后所有智能节点只读继承
- **运行统计与轨迹**：轮数 / LLM 与工具用时 / 首 token 平均 / tok/s / 缓存命中率 / token 用量 / 子代理数；节点内指标行 + 工具轨迹折叠 + 「上下文分布」明细弹窗 + 状态栏统计
- **交互面板**：引擎提问（ask_user）与权限审批（approval）右下角悬浮卡片实时弹出，支持单选 / 多选 / 自定义回答
- **设置窗口新增**：
  - 权限预设：无人值守 / 工作区读写·逐项审批 / 只读·逐项审批 / 完全放行（热重载）
  - **技能（Skills）**：创建（kebab-case 名称 / 描述 / Markdown 内容）、移除，安装后智能节点自动发现
  - **MCP 服务器**：stdio / streamable-http 两种传输，命令 / 参数 / URL，启停 / 移除
  - 插件管理改版：可搜索双列卡片（挂载点 / 状态标签 / 详情）
  - **10 款主题色**：工业（默认）/ 翡翠 / 紫晶 / 玫瑰 / 天青 / 琥珀 / 青柠 / 樱花 / 月光 / 深红，即时预览
  - 任务完成音效开关（内置 Web Audio 双音提示，运行超 5 分钟才播放）
- 输出面板上限从 440px 放宽到 120–2000px，拖拽时同步节点宽度防挤压
- 顶栏「更改名称」按钮、工作流下拉（显示节点数）

### 改进与修复

- **真正的中途取消**：`dsh:cancel` 关闭该工作目录引擎运行时，彻底中断在途请求（节点与会话的「终止」均生效）
- 对话节点消息流重做：微信气泡 → dsh 风格透明消息流（角色行 + 思考折叠 + 工具 chips）
- 修复：节点删除后运行状态残留、会话运行结束卡"运行中"、输出面板拖拽后端口位置不刷新、输出节点最小宽度计算
- 新增 IPC：`file:saveText`、`dsh:mcpList/mcpAdd/mcpRemove/mcpSetEnabled`、`dsh:cancel`、`dsh:interact`、`dsh:providerCatalog`、`skill:list/add/remove`

---

## 六、v1.1.1 —— 视觉模型、AI 搭工作流与在线扩展商店

**核心：通用化（脱离 DeepSeek 绑定）+ 图像能力 + 智能协作 + 扩展生态。**

### 新增功能

- **图像输入 + 视觉模型**：
  - 智能任务节点连接图像输入节点后出现「图」徽标，提示用 `@标题` 引用图像
  - 检测到图像输入且未引用时自动追加【已连接图像输入】说明
  - **视觉模型决策**：当前模型支持识图则直接用；否则弹出确认框列出全部视觉模型候选，确认后持续使用（写入 `node.vision`），拒绝则本次不附加图像
  - 模型下拉视觉模型带「图」标记；无视觉模型时 toast 警告
- **AI 助手可直接搭建工作流（画布操作 API）**：
  - 助手会话可发送画布操作事件（create / update / connect / disconnect / remove / group / setWorkflowName / layout）
  - 带限制（最多 40 节点 / 80 更新 / 80 连线）、运行中节点保护、自动写入 `@` 引用、按上游层数自动排版不重叠
  - 完成后提示「智能助手已更新画布：创建 X 个节点 · 连接 X 条线 · 已排版」
- **在线扩展商店**：设置中「在线浏览」→ 插件 / 技能 / MCP 三个内置源（npmmirror 搜 `@deepseek-ai/dsh` 插件、jsDelivr 的 anthropics/skills、modelcontextprotocol/servers）；卡片式搜索、已安装标记、一键安装 / 卸载；支持自定义源（npm search / jsDelivr repo + 子目录）
  - 主进程新增 `net:fetch` IPC 代取远程内容（绕过 CORS，20 秒超时）
- **自定义任务完成音效**：mp3 / wav / ogg / m4a 文件 + 试听 / 清除，无自定义时回退内置双音
- **添加服务商对话框**：目录模式（从 DeepSeek 官方 / pi-ai 目录选服务商 → 填 Key → 自动载入模型与 baseUrl）或手动模式
- **API Key 安全**：设置中 Key 输入框改 password 类型 + 复制按钮

### 改进与修复

- **智能能力不再限定 DeepSeek**：任意带 API Key 的文本服务商（OpenAI 兼容）均可启用智能能力；设置中移除「启用智能能力」总开关
- 供应商 / 模型下拉只显示已添加项，失效值自动回退 deepseek-official；更换供应商 / 模型后重新评估视觉模型
- 设置面板布局重排（插件 / 技能 / MCP 标题与按钮同行，「已安装插件」折叠收纳）
- 输出面板改左侧边缘竖条拖拽（← 拉宽 · → 收窄）
- 连线错误提示细化（`connectError`：不接受输入 / 回路 / 已连接 / 图像保存节点仅 1 输入等）
- 修复：窗口失焦吞 mouseup 导致的拖拽状态残留与偶发报错（新增 `cancelDrag`）
- 帮助文档重写「⑪ 智能能力」，新增「让助手搭工作流」说明

---

## 八、v1.1.2 ~ v1.1.25 —— 超级节点与画布收纳

**核心：用超级节点收纳子图、边端子隧穿、子文件夹相对路径，以及边栏树状快速进入。**

### 超级节点（`kind: "super"`）

- **收纳子图**：右键添加，或框选后顶栏「超节点」一键合并（覆盖选区外框）
- **展开壳层**：标题栏展开图标（在「进入」右侧），主画布上编辑内部节点；展开时内容贴齐舞台左上
- **进入 ↪**：全屏内部画布，面包屑返回；与任务进入体验一致
- **可嵌套**：超级节点可再收纳超级节点；拖入 / 拖出移入移出，含环检测
- **边端子隧穿**（无内部输入/输出代理节点）：
  - 外侧输入 / 输出（收起时）
  - 内侧桥接（左）/ 汇流（右）：展开壳层边缘，或进入后钉在画布左右边缘
- **子文件夹**：标题栏设置；内部保存 / 音视频 / 监视等待等相对路径自动落在 `工作目录 / 子文件夹 / …`；移入或改子文件夹时自动补前缀
- **排版**：一键排版可询问是否同时整理超级节点内部
- **边栏树**：☰ 下方「超级节点」树状列表，点击进入并适配内部节点；与「当前画布」列表分隔

### 文档

- 手册新增 [超级节点](#super-nodes)；界面导览 / 工作目录 / 框选与组 / FAQ / 节点指南同步更新

---

## 八·补、v1.1.26 ~ v1.1.28 —— 开发节点、数据库节点与更多节点类型

> v1.1.26–v1.1.28 未按小版本逐一记录，本节汇总**当前构建（1.1.28）**相对 v1.1.25 的全部增量。

### 开发节点（功能块，`super` + `dev:true`）

- **软件项目架构画布**：元素层级 模块（绿）→ 文件（蓝）→ 类 / 接口 / 枚举（橙 / 紫 / 粉），不同外框颜色区分；`devPath` 项目根目录（顶层块设置、子块继承）；`devStatus` pending / wip / done；`parentSuperId` 剥洋葱式嵌套，一次只细化一层。
- **关系线（rel）**：元素之间用关系线表达依赖 / 调用 / 实现（**普通直线**、可带 `relLabel` 文字与 `relArrow` 箭头），不参与执行。
- **关系线显示修复**：展开壳层内的关系线此前会被壳层自己的连线清理当成「残留」删掉线芯（只剩外圈与文字，箭头随之消失），且多个壳层共用一份全局箭头 `marker`。现在关系线在**所有普通连线刷新之后**按层级统一绘制一次；箭头 `marker` 按壳层独立、`userSpaceOnUse` 固定尺寸；`forward / backward / both / none` 四种箭头方向都正确显示，选中 / 关联节点时箭头一起高亮。
- **关系线走线算法**：同层关系线**整体规划**——进出点取「中心连线与方块边框的交点」、同一节点同一侧的多条线**扇形分散锚点**、两条几乎重合的直线**沿各自的边滑开**直到可分辨（采样距离判定），线上文字自动避让。**不再使用直角折线 / 车道绕行，一律普通直线。**
- **汇聚锚点两趟收敛（少一类 X 形交叉）**：只按「对端方块中心」排扇形顺序时，两条线进出同一块方块的先后可能颠倒，视觉上仍然互相穿越。现在第一趟按中心定序，随后最多两趟改按**对端真实来向（上一趟算出的锚点）**重排，并只在「直线两两交叉的对数确实变少」时接受，否则回退到当前最优（不会来回抖动）。真实架构图实测交叉 **4 → 3**，样例图 **4 → 0**；线数超过 120 的层级跳过收敛，拖拽时每帧成本仍可忽略。
- **点选节点高亮相关关系线**：点击任意开发节点（含展开壳层内的功能块）后，与它相关的关系线立即亮起（青色 + 光晕，箭头同步换色），与它无关的关系线淡出退到背景；点线本身仍是橙色「选中这条线」，点空白取消。孤立块（没有连着任何关系线）被选中时不会淡化整张图。
- **开发节点排版修复**：自动排版此前**把关系线整条丢弃**，纯关系线的架构图退化成「5 个一行」的方块网格、关系线全部叠在一起。现在关系线按箭头方向**参与分层**（会让图成环的边、双向 / 无箭头边自动降级为只影响顺序的软约束），减交叉排序考虑跨层长边，含关系线的图自动放宽走廊（列间 ≥196px、分量间 ≥208px）。
- **展开壳层按内容撑大**：细化 / AI 批量建节点 / 收纳子块后，壳层尺寸跟随内容范围（不再被默认 720×480 舞台裁掉）；AI 批量建节点时也改为**在各自层级内**排版（壳层内用舞台局部坐标，不会把子块丢到舞台外）。
- **开发节点右键新增「按关系线整理内部排版（分层 · 可撤销）」**：递归整理该功能块及其子壳层（由内向外），排版后按内容收紧壳层。真实架构图样本（17 块 / 21 条关系线）实测：共线叠线段 **45 → 17**，完全重合线 0 条。
- **「建议」按钮（AI 主动评估下一步）**：点「建议」→ 确认对话框（显示元素类型 / 开发状态 / 项目根 / 下层元素 / 历史会话 / 上次建议 + 可选关注点）→ AI **只读**调研项目真实代码与该模块开发进度（硬禁写文件 / 改画布）→ 就地返回**恰好 4 条**方案（标题 + 说明 + 优先级）→ 可**任意多选 + 补充说明** → 点同一对话框里的「开发」即按所选方案在**新建绑定会话**（工作区 = 项目根）中开工。结果缓存到节点（查看上次建议不重跑，「换一批」才重跑）。
- **「开发 / 细化」按钮**：均先弹对话框确认；开发在绑定会话运行并回写节点概述与状态；细化先报内容梗概、经确认才创建子节点，无需或无法细化时明确告知。
- 三个按钮入口齐全：节点头部 chip、折叠卡按钮、右键菜单；「建议 / 开发 / 细化」确认框都会列出**当前生效的 Agent 模型**。
- **节点自定义颜色（`devColor`）**：元素类型徽章旁新增**颜色小按钮**（圆点 = 当前色），点击展开 **HSV 色板**（S×V 方块 + 色相条，也可直接输入 Hex），改的是该功能块的外框与运行呼吸灯颜色；「恢复元素类型默认色」回到类型配色。
- **Agent 模型选择（`devModel` / `devProvider`）**：菜单栏再加一个 **🧠 模型按钮**（显示当前生效模型，未选 = 「自动」，虚线 = 继承上层），点击展开按**服务商分组**的模型清单。选定后，本功能块的「建议」只读调研与「开发 / 细化」绑定会话都走这个模型；**未自行选择的子功能块就近向上继承**——在顶层功能块设一次即全项目统一，子块自己选过则以子块为准；「跟随默认（不指定）」清除。折叠卡常驻一行生效模型（继承时注明来源块），点外部 / 再点按钮 / Esc 收起；`mtnode_canvas_get` / `mtnode_canvas_edit` 均可读写 `devColor` / `devModel`。
- **执行节点**外观收敛：body 不再重复显示标题（标题只留在节点头部），图标居中放大，路径行与两段式播放键更紧凑。
- 开发节点冒烟测试 **294 项**通过（契约解析 / 进度上下文 / 只读纪律 / 全流程 / 缓存 / 键盘 / 异常 / 接线 / 运行状态 / 颜色与模型）。

### 数据库节点（`super` + `db:true` → `db_replica`）

- 设置开启「**测试版本（Beta）**」后，右键可添加「数据库」超级节点（默认子文件夹 `db/`）；内部右键只提供**文件节点**（批量导入任意文件，复制进子文件夹）与**表节点**（Agent 抽元数据 → 用户确认表单 → SQLite / FTS5 建表）。
- 头部 **⚙ 编译**（仅手动、增量）：索引子文件夹文件 + 内部信息节点，生成 / 刷新**数据库副本**子节点；头部 ▤/▦ 切换 超级节点形态 ↔ 数据库形态（内嵌查询 / calc / 检查更新调试工具）。
- 智能节点接入：连线副本到输入端（自动注入「数据库接入 · 事实强制约束」）或 prompt 写 `!@数据库标题`；用 **`mtnode_db`** 工具 list / query / get / calc 查询，强制事实纪律（断言带 `[记录id · 标题]` 溯源、查不到答「数据库中没有该信息」、推断标注「数据库未记载」、数字走 calc）。

### 其它新节点

- **执行节点（`execute`）**：绑定 `.exe` / `.bat` / `.cmd` / `.lnk` 或任意系统可打开文件；双击节点或连点两下播放键即通过系统默认方式启动；`execIcon` / `execColor` 自定义外观便于定位。创建入口在右键「开发节点」菜单内（与模块 / 文件 / 类 / 接口 / 枚举并列）；也可在功能块上右键「在内部新建执行节点（启动器）」，把启动器直接放进该模块。
- **音乐生成（`music_gen`）**：MiniMax Music 3（Gradio）本地后端；端子 0 = 提示词 · 1 = 歌词 · 2 = 控制；每次运行产出一个 `.wav` 到自带输出路径；抽卡次数、种子（每次 +1）。
- **视频生成（`video_gen`）**：MiniMax H3（ComfyUI）本地后端；`fl2va` 首末帧（默认）/ `r2v` 多参考图；时长 4–15 s；分辨率 auto / 480p / 720p / 1080p（显存不足自动降档）；4K 超分补帧（24G 建议关）；每次运行产出一个 `.mp4`。
- **网络节点（`net_recv` / `net_send`）**：TCP / UDP 文本通道；通道号（0–65535）同端口多路复用；逐节点 host / port，全局默认端口在 设置 · 网络（接收 40999 / 发送 41000）；接收节点默认自动监听。
- **全局节点（`global`）**：只连入、不连出，广播文本 / 图像源；消费者需同时开启引用（`globalRefs`）并在提示词写 `@源标题`。
- **控制节点补齐**：定时触发器支持 一次 / 间隔 / Cron；延时器、序列器、闸门、分发、计数、互斥、判断、需求等待齐全，统一金色外圈。

### 其它改进

- **端子文字移到节点外侧**：端子上下只隔 12px，原先写在端子上方 / 插排顶部的文字（`输入` / `输出`，以及音乐 `提示词` / `歌词` / `控制`、视频槽位号、闸门 / 互斥序号、任务 `成功` / `失败`…）会盖住相邻端子。现在一律写在**节点板外侧、与端子同一水平线**：输入侧出左、输出侧出右；展开的超级节点 / 开发节点壳层同样把 `输入` / `输出` 标在壳层左右外缘（与内侧第一孔对齐），某一侧没有端子时不再显示该侧文字。
- **插件目录本地合并**：云端目录缺项时随包内置插件（TTS / Llama / Music3 / H3 / 桌宠 / 讨论区）自动补回；插件图标支持自定义（`tts-local.png` 等，512×512 / ≤2 MB）。
- 应用内手册新增 [开发节点](#dev-nodes)、[数据库节点](#database-nodes)、[音乐、视频、网络与执行节点](#media-net) 三页；节点指南补齐 `input_file` / `db_table` / `db_replica` / `global` / `music_gen` / `video_gen` / `net_recv` / `net_send` / `execute`。

---

## 九、升级路径与兼容说明

- **v1.0.25 存档可无缝升级**：`migrateWf` 自动补齐各版本新增字段（`agent`、`batchMode`、`workspace`、`preset`、`effort`、`agentSessionId`、`wf.workspace`、`superOpen` / `subFolder` / `parentSuperId` 等）
- **智能能力前置条件**：v1.0.26–v1.0.28 需 DeepSeek 服务商；**v1.1.1 起**任意带 API Key 的 OpenAI 兼容文本服务商即可
- **Node 运行时**：v1.0.27 起随安装包内置，无需单独安装
- **旧版「默认工作目录」**：v1.1.0 起改为工作流级统一工作目录（画布工具栏设置）
- **超级节点**：旧版若曾使用内部 `super_io` 代理节点，加载时会迁移为边端子连线
- **数据库节点**：属测试功能，需在设置开启「测试版本（Beta）」后才会出现在添加节点菜单
- **开发节点 / 数据库节点**：需要 `canvas_super` 权限（智能会话 / 助手改画布时确认）

---

_本文档由 v1.0.25 起变更汇总；当前产品版本见应用内 `version`（如 v1.1.28）。_
